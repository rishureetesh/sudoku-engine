# Contributing

```bash
npm install
npm test
npm run typecheck
npm run lint
npm run build
```

Add tests under `src/games/sudoku/engine/tests/` next to what you changed.

Open a PR against `main`. CI needs to pass.
